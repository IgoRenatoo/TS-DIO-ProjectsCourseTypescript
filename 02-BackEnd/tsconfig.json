{
  "compilerOptions": {
    "target": "ES6", // Define a versão ECMAScript para a qual o TypeScript será compilado (ES6).
    "module": "CommonJS", // Define o sistema de módulos como CommonJS, utilizado principalmente em projetos Node.js.
    "outDir": "./dist", // Especifica o diretório de saída para os arquivos compilados (neste caso, 'dist').
    "rootDir": "./src", // Define o diretório raiz para os arquivos TypeScript de entrada (neste caso, 'src').
    "strict": true, // Habilita a verificação rigorosa do sistema de tipos, incluindo verificações de null, undefined, etc.
    "esModuleInterop": true, // Habilita a interoperabilidade entre módulos ECMAScript e CommonJS, permitindo o uso de `import` e `require`.
    "experimentalDecorators": true, // Ativa o uso de decoradores, um recurso experimental do TypeScript.
    "emitDecoratorMetadata": true, // Emite metadados de tipo necessários para trabalhar com decoradores, como no NestJS.
    "allowJs": true // Permite a inclusão de arquivos JavaScript (com extensão .js) no processo de compilação.
  },
  "include": ["src/**/*.ts"], // Inclui todos os arquivos TypeScript (extensão .ts) na pasta 'src' e subdiretórios.
  "exclude": ["node_modules"] // Exclui a pasta 'node_modules' do processo de compilação para evitar compilar dependências.
}
